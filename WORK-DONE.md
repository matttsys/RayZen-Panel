# WORK-DONE — RayZen-Panel

Labels: **VERIFIED** = I ran it and read the output. **INSPECTED** = I read the code, nothing
executed it. **NOT DONE** = not attempted, or nothing to do.

## What I ran

- `npx tsc --noEmit` — clean. **VERIFIED**
- `npx vitest run` — 41 files, 1193 tests, 0 failures. **VERIFIED**
- `npm run build` — Worker built. **VERIFIED**
- `npm run check:doc-links` — 61 markdown files, 77 relative links, 0 broken. **VERIFIED**
- `node scripts/design-token-audit.js` — 0 failures. **VERIFIED**

This tree is the **local worktree** copy from the bundle, not `origin/main`. `CHANGES.md`
section 4 item 12 records that the worktree carried the Companion integration work
(`src/api/platform.ts` +213, `panel/script.js` +246, `style.css` +135) that the shallow clone
did not. I deleted the stale clone and built on the worktree. **VERIFIED** (the clone had no
`clean-ips`, `subscriptions/urls` or `companionApi: 2`; the worktree does).

## Files changed

| File | Why |
| --- | --- |
| `src/features/analytics/traffic.ts` | **New.** Hourly traffic buckets under `rz:metrics`: byte totals, request count, per-hour-of-day profile, derived 3-hour peak period. Counts accumulate in isolate memory and flush at most once per 5 minutes or 50 requests. Response bytes are measured with a counting `TransformStream` because most Worker responses are streamed and carry no `Content-Length`. |
| `src/platform/repositories.ts` | Added `RETENTION.trafficHours = 168`; declared `hours` on `MetricsDocument`; made `increment` and `clear` spread the current document so they no longer drop fields the traffic module writes into the same KV value. |
| `src/worker.ts` | Extracted the routing body into `route()`; `fetch` now wraps the response in the byte counter and calls `flushTraffic` once per request. No per-request KV write. |
| `src/handlers/panel.ts` | Added `panel/metrics` (GET reads the traffic snapshot, DELETE clears it), auth-gated. Extended the `panel/version` capability set with `scannerApplyBatch`, `scannerCustomRange`, `cleanIpManagement`, `modes`, `trafficMetrics`. |
| `src/api/platform.ts` | Removed the 40-item cap (`MAX_CLEAN_IPS` 40 → 1000, a settings-size bound; `MAX_SCANNER_APPLY_BYTES` 4 KB → 64 KB). Rewrote `scanner/apply` to take an arbitrary-length array, dedupe, validate each entry as a published Cloudflare IPv4, and return `accepted` plus `rejected[{address,reason}]` instead of failing on the first bad entry. `clean-ips` gained `PUT` (verbatim ordered store — add, delete and reorder in one write) and `DELETE`. New `modes` route reading and writing `proxyIpMode` and `fragmentMode`, each to its own KV document. `scanner/candidates` gained `?range=<cidr>`. |
| `src/features/scanner/candidates.ts` | Added `validateCloudflareRange` (full containment in the published list, /8–/31, typed rejection reason) and `candidatesInRange`. |
| `src/assets/panel/style.css` | Removed ASCII banner rules from comment headers. |
| `docs/INTEGRATION-CONTRACTS.md` | Rewritten to describe what the code does at `companionApi: 2`. No v1 section, no migration notes. |
| `tests/unit/platform-api.test.ts` | Updated for the v2 contract (the old expectations pinned the removed 40-cap and the old response shape) and added cases for partial acceptance, a 300-address apply, ordered `PUT`, `DELETE`, `modes` and range validation. No test was removed. |
| `.github/workflows/build.yml` | `node-version` 22 → 20.10 so CI runs the version `package.json engines` and `README.md` both declare. Removed the `actions/upload-artifact` step that stored ~560 KB of `dist/worker.js` on every push. |

## Files deleted

| File | Why |
| --- | --- |
| `dist/` (regenerated by `npm run build` during my checks, excluded from the zip) | Committed build output. |
| `GIT-STATE.txt` | Handover artifact from the bundling pass, not part of the repository. |
| `../RayZen-Panel` (the shallow `origin/main` clone) | Superseded by the local worktree, which carries the integration work. Not part of the delivered zip either way. |

## Part 1 items owned by the Panel

11. Junk files — `donations.txt`, `opencode.json`, `TDZ-INCIDENT-REPORT.md`, logs, `local.properties`,
    `.DS_Store` and AI-agent config: **none present** in this tree (the worktree had already
    staged the TDZ report and `dist/` for deletion). Deleted `dist/` and `GIT-STATE.txt`. **VERIFIED** by `find`.
12. AI-flavoured comments, ASCII banners, stale TODOs, dead code — stripped the `=====` banner rules
    from `panel/style.css`. A grep for `TODO|FIXME|XXX|HACK|for now|temporarily` across `src/`
    returns nothing. **VERIFIED**. I did not do a line-by-line prose audit of the 5,285-line
    `panel/script.js`; **NOT DONE**.
14. Dead CHANGELOG links — **NOT REPRODUCED.** `npm run check:doc-links` reports 0 broken across
    61 files, and a grep for `CHANGELOG` in the README, both mkdocs configs, `RELEASE.md` and
    `CONTRIBUTING.md` returns nothing, so there is no link to fix. **VERIFIED**.
    Node version aligned and the artifact upload removed as described above. **VERIFIED**.

## Part 2 items owned by the Panel

- **A — Apply All (server side).** `scanner/apply` takes any length, dedupes, validates each entry as
  Cloudflare IPv4, returns accepted count and per-entry rejection reasons. **VERIFIED** by tests,
  including a 300-address apply.
- **B — Custom range (server side).** `scanner/candidates?range=` validated against the published
  IPv4 list, overlapping prefixes refused. **VERIFIED** by tests.
- **C — Clean-IP management (server side).** `GET`/`PUT`/`POST`/`DELETE clean-ips`, one KV write per
  save, order preserved. **VERIFIED** by tests.
- **D — Mode switching (server side).** `modes` route. **VERIFIED** by tests, including that
  `proxyIpMode` lands in `rz:identity` and `fragmentMode` in `proxySettings`.
- **E — Metrics.** Implemented as described. **VERIFIED** for typecheck, build and the existing test
  suite; the accumulator's flush thresholds are **INSPECTED**, not exercised against a live KV
  namespace — I did not run `wrangler dev`, so there is no runtime evidence that a real deployment
  writes once per fifty requests rather than more often.
- **I — Contract doc.** Rewritten. **INSPECTED** against the code route by route.

## Known gaps

- The panel's own HTML UI was **not** given controls for clean IPs or modes. The operator's
  instruction was that those surfaces live in the Companion app, so the Panel side is the API and
  the Companion renders it. `src/assets/panel/index.html` already has a clean-IP input; it was not
  rewired to the new `PUT`/`DELETE` semantics. **NOT DONE.**
- `wizard/artifacts/worker.js` (556 KB) is still committed. It is build output, but
  `npm run release:sync-wizard` regenerates it and the wizard serves it at runtime, so deleting it
  without rewiring the wizard would break `npm --prefix wizard test`. **NOT DONE**, deliberately.
- WebSocket payload bytes and request bodies with no `Content-Length` are not counted, and the doc
  says so.
- No `wrangler deploy`, no live KV. Everything above is typecheck, unit tests and code review.

## Secrets

No key, keystore, token or password was added. The two pre-existing `password:"` matches in
`wizard/artifacts/worker.js` are a storage-key name and a `typeof` comparison, as `CHANGES.md`
section 8 records; nothing was printed or removed.
