{
  "name": "rayzen-deployment-wizard",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && for f in lib/*.js api/*.js api/*/*.js; do node --check \"$f\"; done",
    "vercel-build": "node scripts/sync-font.mjs"
  }
}
